# Calendar / Запись на занятия

Минимальный прототип веб-приложения для записи на занятия.

## Что уже реализовано

- Регистрация пользователя: имя, фамилия, адрес, телефон, пароль.
- Вход по телефону + паролю.
- Создание админ-аккаунта на странице регистрации через галочку и секретный код.
- Просмотр свободных слотов.
- Календарь дат: по клику на день открывается окно со свободным временем.
- Запись в свободный слот.
- Отмена своей записи.
- Удаление своей записи.
- Для администратора:
  - просмотр всех записей;
  - просмотр раздела "Заблокированное время";
  - блокировка/разблокировка слотов;
  - блокировка дня целиком;
  - просмотр и удаление зарегистрированных пользователей на отдельной странице-таблице;
  - смена секретного кода для регистрации админов через модальное окно;
  - настройка email для уведомлений на странице «Связь»;
  - отправка уведомления администратору при новой записи.

## Запуск

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
export FLASK_SECRET_KEY='replace-me'
export ADMIN_SECRET_CODE='my-secret-admin-code'
export SITE_EMAIL_ADDRESS='propdd38@proton.me'
export SITE_EMAIL_PASSWORD='mailbox-or-smtp-password'
# При необходимости настройте SMTP_HOST, SMTP_PORT и SMTP_USE_SSL.
python app.py
```

Сайт будет доступен на `http://localhost:5000`.

## Технические заметки

- База данных: `SQLite` (`app.db`), создаётся автоматически при первом запуске.
- Слоты автоматически поддерживаются актуальными: с текущей даты и на 14 дней вперёд (время `10:00`, `12:00`, `14:00`), прошедшие удаляются.
- Запись на занятия в текущий день отключена.
- Email-пароль не хранится в коде: задайте его через переменную окружения `SITE_EMAIL_PASSWORD`.
- Endpoint `POST /delete/<slot_id>` удаляет запись пользователя из его списка и освобождает слот (слот не удаляется из расписания).
